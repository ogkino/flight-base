# 环境变量配置模板
# 复制此文件并重命名为 .env（如果需要使用环境变量）

# 数据库配置
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=flight_base
DB_USER=root
DB_PASS=root
DB_PREFIX=og_
DB_CHARSET=utf8mb4

# 应用配置
APP_NAME="Flight Base"
ADMIN_NAME="Flight Base 管理后台"
APP_DEBUG=true
APP_TIMEZONE=Asia/Shanghai

# Token 过期时间（秒）
TOKEN_EXPIRE=604800

# 日志配置
LOG_ENABLED=true
LOG_LEVEL=info
